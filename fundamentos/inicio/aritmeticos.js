const [a,b,c,d,] = [3,5,1,15]//forma reduzida de criar 4 variaveis.

const soma = a+b+c+d //Aqui tem um operador aritmeticos, opera sobre dois operandos.
/*posso pegar a var "a", e tornar negativa,basta, -a  */
const subtracao = d-b
const multiplicacao = a*b
const divisao= d/a
const modulo = a%2 // resto da divisao por 2. Usado para saber se um número é par: sempre 1 ou 0

console.log(soma,subtracao,multiplicacao,divisao,modulo)
//posso utilizar o operador unário.
console.log(-subtracao)//converti em negativo.

//operador aritimético é um operador binário. Porque existem dois operando.