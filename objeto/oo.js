//CÓDIGO NÃO EXECUTAVEL!!
/*grande guarda chuva: linguagem estruturada. dentro disso temos o procedural. ( que é a possibilidade de criar uma função e utilizar
ela depois) */

//procedural. (funções que manipulam dados)
processamento (valor1,valor2,valor3)

//OO (tem dados e dentro deles tem funções.)
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        //...
    }
}
objeto.processamento()
//quem tem o processamento é o objeto

//principios de orientação a objeto
// (1) abstração
// (2) encapsulamento (informações que ficam ocultas, que não são necessarias)(como um carro queima combustivel)
//tornar o sistema encapsulado é melhor: evitar um grande número de depedência.(entre os objetos)
//nome técnico: alto nivel de acoplamento.

//(3) herança(prototype)= ( é um)o filho herdar caracteristicas do pai.
//priorizar a COMPOSIÇÃO em vez da HERANÇA
//(4) polimorfismo (multiplas formas)

// o fracionamento(modo de reutilizar código). de por Ex: Carro: dividido em várias partes, e todas juntas forma o carro. Eu não preciso colocar os elementos 
//que formam o motor , escreverem todos dentro de carro. ( é a possibilidade de utilização de elementos menores, e sua complexidades de forma
//separada)